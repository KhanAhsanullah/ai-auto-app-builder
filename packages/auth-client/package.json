{
  "name": "@ai-commerce/auth-client",
  "version": "0.0.0",
  "private": true,
  "description": "Authentication client library: OAuth/PKCE flows, token refresh, secure storage adapters for web and React Native.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "echo \"dev not configured yet\"",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  }
}
