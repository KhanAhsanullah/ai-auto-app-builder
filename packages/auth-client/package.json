{
  "name": "@ai-commerce/auth-client",
  "version": "0.0.0",
  "private": true,
  "description": "Authentication client library: tenant auth policy resolution, provider ports, OAuth/PKCE, token refresh, and secure storage adapters.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "dev": "echo \"dev not configured yet\"",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@ai-commerce/config-schema": "workspace:*"
  },
  "devDependencies": {
    "@ai-commerce/config-runtime": "workspace:*",
    "@types/node": "^22.10.2",
    "vitest": "^3.0.5"
  }
}
