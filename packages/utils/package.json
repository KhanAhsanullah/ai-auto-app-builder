{
  "name": "@ai-commerce/utils",
  "version": "0.0.0",
  "private": true,
  "description": "Shared pure utility functions with no domain coupling. Used across packages, modules, and applications.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "echo \"dev not configured yet\"",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  }
}
