{
  "name": "@ai-commerce/feature-flags",
  "version": "0.0.0",
  "private": true,
  "description": "Feature flag resolution from tenant configuration and runtime overrides. Enables gradual rollout per tenant and vertical.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "echo \"dev not configured yet\"",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  }
}
