{
  "name": "@ai-commerce/white-label-engine",
  "version": "0.0.0",
  "private": true,
  "description": "White-label brand resolver transforming tenant branding configuration into resolved brand identity for all surfaces.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./internal": "./src/internal.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "dev": "echo \"dev not configured yet\"",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@ai-commerce/config-schema": "workspace:*"
  },
  "devDependencies": {
    "@ai-commerce/config-runtime": "workspace:*",
    "@types/node": "^22.10.2",
    "vitest": "^3.0.5"
  }
}
