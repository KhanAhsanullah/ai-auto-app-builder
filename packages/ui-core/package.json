{
  "name": "@ai-commerce/ui-core",
  "version": "0.0.0",
  "private": true,
  "description": "Headless UI primitives and state machines (cart, checkout, forms). Platform-agnostic business UX logic shared between web and mobile.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "echo \"dev not configured yet\"",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  }
}
