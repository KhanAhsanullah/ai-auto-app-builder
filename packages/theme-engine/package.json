{
  "name": "@ai-commerce/theme-engine",
  "version": "0.0.0",
  "private": true,
  "description": "Design token compiler transforming brand configuration into CSS variables, Tailwind presets, and React Native theme objects for all surfaces.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "echo \"dev not configured yet\"",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  }
}
