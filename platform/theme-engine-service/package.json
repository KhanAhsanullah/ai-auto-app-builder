{
  "name": "@ai-commerce/theme-engine-service",
  "version": "0.0.0",
  "private": true,
  "description": "Control-plane service that compiles theme JSON into deployable artifacts: CSS bundles, RN themes, favicons, and splash assets.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "echo \"dev not configured yet\"",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  }
}
