{
  "name": "@ai-commerce/tenant-provisioner",
  "version": "0.0.0",
  "private": true,
  "description": "Control-plane service for tenant onboarding, seed data injection, and initial vertical template application.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./internal": "./src/internal.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "dev": "echo \"dev not configured yet\"",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@ai-commerce/config-runtime": "workspace:*",
    "@ai-commerce/config-schema": "workspace:*"
  },
  "devDependencies": {
    "@ai-commerce/theme-engine": "workspace:*",
    "@ai-commerce/white-label-engine": "workspace:*",
    "@types/node": "^22.10.2",
    "vitest": "^3.0.5"
  }
}
