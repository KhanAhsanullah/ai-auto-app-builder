/** Platform plugin engine API version checked against manifest engineVersion ranges. */
export const PLUGIN_ENGINE_API_VERSION = '5.0.0';

/** Filename suffix for filesystem plugin manifest discovery. */
export const PLUGIN_MANIFEST_FILE_SUFFIX = '.plugin-manifest.json';
