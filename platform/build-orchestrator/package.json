{
  "name": "@ai-commerce/build-orchestrator",
  "version": "0.0.0",
  "private": true,
  "description": "Control-plane service that generates and deploys tenant-specific web, admin, and mobile artifacts on config publish.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "echo \"dev not configured yet\"",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  }
}
