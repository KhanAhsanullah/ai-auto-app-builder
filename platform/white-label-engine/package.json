{
  "name": "@ai-commerce/white-label-engine-service",
  "version": "0.0.0",
  "private": true,
  "description": "Control-plane service for custom domains, SSL provisioning, app identity (bundle IDs), and branded legal/comms templates.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "echo \"dev not configured yet\"",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  }
}
