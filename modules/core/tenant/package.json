{
  "name": "@ai-commerce/module-tenant",
  "version": "0.0.0",
  "private": true,
  "description": "Core domain module for organizations, stores, locales, domains, and subscription tiers. Root of multi-tenancy; all data scopes from here.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "echo \"dev not configured yet\"",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  }
}
