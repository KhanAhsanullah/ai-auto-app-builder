{
  "name": "@ai-commerce/module-payment",
  "version": "0.0.0",
  "private": true,
  "description": "Core domain module for payment gateways, webhooks, capture, and reconciliation. Centralized PCI-sensitive operations.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "echo \"dev not configured yet\"",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  }
}
