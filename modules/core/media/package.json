{
  "name": "@ai-commerce/module-media",
  "version": "0.0.0",
  "private": true,
  "description": "Core domain module for file upload, CDN URL generation, and image transformations. Shared asset pipeline for catalog and branding.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "echo \"dev not configured yet\"",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  }
}
