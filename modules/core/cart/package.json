{
  "name": "@ai-commerce/module-cart",
  "version": "0.0.0",
  "private": true,
  "description": "Core domain module for shopping cart lines, promotions application, and tax preview. Entry point to the checkout pipeline.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "echo \"dev not configured yet\"",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  }
}
