{
  "name": "@ai-commerce/api-gateway",
  "version": "0.0.0",
  "private": true,
  "description": "Backend-for-Frontend (BFF) and edge API gateway. Handles routing, aggregation, rate limiting, and tenant context injection for web and mobile clients.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "echo \"build not configured yet\"",
    "dev": "echo \"dev not configured yet\"",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  }
}
