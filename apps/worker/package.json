{
  "name": "@ai-commerce/worker",
  "version": "0.0.0",
  "private": true,
  "description": "Background worker service for async jobs: webhooks, config rebuilds, notifications, inventory sync, and event-driven workflows.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "echo \"build not configured yet\"",
    "dev": "echo \"dev not configured yet\"",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  }
}
