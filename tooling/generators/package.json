{
  "name": "@ai-commerce/generators",
  "version": "0.0.0",
  "private": true,
  "description": "Code and manifest generators that scaffold app surfaces from tenant configuration. Used by build-orchestrator and CLI.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "echo \"dev not configured yet\"",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  }
}
