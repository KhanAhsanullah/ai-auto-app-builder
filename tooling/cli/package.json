{
  "name": "@ai-commerce/cli",
  "version": "0.0.0",
  "private": true,
  "description": "Command-line interface for tenant creation, config validation, artifact generation, and local development workflows.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "echo \"dev not configured yet\"",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  }
}
